[phases.setup]
nixPkgs = ["tor", "curl"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "pip install stem>=1.8.2"
]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "./start.sh"
